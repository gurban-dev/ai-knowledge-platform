# Railway service config — Web (Next.js)
[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/Dockerfile.web"

[deploy]
startCommand = "pnpm --filter @akp/web start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
